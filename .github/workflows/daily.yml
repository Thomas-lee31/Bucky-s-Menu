name: Daily Menu Job

on:
  # Run once daily at 4:00 AM Central Time (10:00 UTC during DST, 9:00 UTC during standard time)
  # Using 10:00 UTC to account for Central Daylight Time
  schedule:
    - cron: "0 10 * * *"
  # Allow manual runs from the GitHub UI
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  run-daily:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repo
        uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npx prisma generate

      # Test connection and deploy schema
      - name: Test database connection
        run: |
          echo "Testing basic connectivity..."
          timeout 30 bash -c "</dev/tcp/db.egwytzjayaviridmhrkb.supabase.co/5432" && echo "Port 5432 is reachable" || echo "Port 5432 is NOT reachable"
        continue-on-error: true
        
      - name: Deploy schema to Supabase
        run: |
          echo "Deploying schema..."
          npx prisma db push --skip-generate --accept-data-loss
        timeout-minutes: 5
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}

      # Run your daily job (uses tsx)
      - name: Run daily menu job
        run: npm run job:daily
        timeout-minutes: 5
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
          # SENDGRID_API_KEY: ${{ secrets.SENDGRID_API_KEY }}
          # Add more envs as needed, e.g. NUTRISLICE_BASE, etc.

      # (Nice-to-have) Upload a log/summary artifact
      - name: Save job summary
        if: always()
        run: |
          echo "Run at: $(date -u)" >> job-summary.txt
          echo "Node: $(node -v)" >> job-summary.txt
          # If your script writes its own summary to a file, append it here.
        shell: bash
      - name: Upload summary artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: daily-job-summary
          path: job-summary.txt
